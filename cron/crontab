WORK_DIR="@WORK_DIR@"
CONFIG_FILE="@CONFIG_FILE@"
@CRON_PERIOD@ $WORK_DIR/run.sh  > /proc/1/fd/1 2>/proc/1/fd/2
# Don't remove the empty line at the end of this file. It is required to run the cron job